public class InformationHidingEncapsulation {
    // Information Hiding:
    /*
     * Cannot change the stuff inside a class (except with methods)
     */

    // Encapsulation:
    /*
     * Hide the details of a class from the user
     * Makes use of information hiding
     */

    // Encapsulation is a mechanism that allows us to hide the details of a class from the user.
    // Public = no restrictions. Private = can only use in class

    // Accessor = get value from class
    // Mutator = change value of instance variable in controlled manner
}
